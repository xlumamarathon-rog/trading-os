__pycache__/
*.pyc
.env
.pytest_cache/
.coverage
*.tar.gz
/vendor/*
!/vendor/MANIFEST.md
# MODULE 60 deployment overlay — never committed
config/brokers_local.yaml
# live/paper runtime state (audit, gate, sentinel) — never committed
data/runtime/
# intraday archive — runtime asset harvested daily by scripts/intraday_archiver.py, never a committed fixture
data/intraday/
